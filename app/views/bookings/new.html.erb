<div class="container mt-4">

  <h1 class="mb-4 text-primary">New Booking for <%= @booking.car.make %></h1>

  <div class="card shadow-sm mb-4">
    <div class="card-body">
      <%= render "form", booking: @booking %>
    </div>
  </div>

  <div>
    <%= link_to "⬅️ Back to Car", car_path(@car), class: "btn btn-outline-secondary" %>
  </div>

</div>
