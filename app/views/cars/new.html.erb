<div class="container mt-4">
  <h1 class="mb-4">Add a New Car</h1>

  <%= render "form", car: @car %>

  <div class="mt-3">
    <%= link_to "← Back to Cars", cars_path, class: "btn btn-link" %>
  </div>
</div>
